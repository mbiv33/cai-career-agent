# @cai/browser — Stage 2 (Track D)

Playwright wrapper: persistent browser profiles per portal, encrypted
credential vault client, form fill, upload, interrupt/resume, escalate on
CAPTCHA/MFA (never bypass). Built in Stage 2 against the contracts in
`@cai/core` and the `portal_credentials` table in `@cai/db`.

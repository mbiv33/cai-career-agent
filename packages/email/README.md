# @cai/email — Stage 3

Gmail API client for the dedicated account (send as Cai, ingest, thread to
Applications). Inbound mail is stored immediately by the Vercel cron poller;
classification (Haiku) happens at the next brain run. Built in Stage 3.

# Local development only. Production: Vercel (web) + Neon (Postgres) + Mac brain runs.
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cai
      POSTGRES_PASSWORD: cai_dev
      POSTGRES_DB: cai_career
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cai -d cai_career"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"

volumes:
  pgdata:
